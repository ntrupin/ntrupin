{% extends "base.jinja" %}
{% block content %}
<section>
  <h1 class="list-page-title">{{ _("404 - Page Not Found") }}</h1>
  <p class="intro-copy">{{ _("The page you are looking for does not exist.") }}</p>
</section>
{% endblock %}
